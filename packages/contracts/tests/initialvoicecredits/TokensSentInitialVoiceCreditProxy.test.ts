// import { Signer } from "ethers";
// import { getDefaultSigner } from "../../ts/utils";

describe("Tokens Sent Initial Voice Credit Proxy", () => {
  /*
  let signer: Signer;

  before(async () => {
    signer = await getDefaultSigner();
  });

  describe("Deployment", () => {
    it("The voice credit proxy should be deployed", async () => {});
  });
  describe("Receive tokens", () => {});
  describe("Send voice credits", () => {});
  */
});
