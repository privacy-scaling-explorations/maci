import * as path from 'path'

const contractFilepath = path.join(__dirname, '..', 'contractAddress.txt')
const contractFilepathOld = path.join(__dirname, '..', 'contractAddress.old')

export {contractFilepath, contractFilepathOld}
