describe('Message processing', () => {
    describe('Process a batch of messages', () => {
        let maciState

        beforeAll(async () => {
        })

        it('TODO: processMessage() should process a valid message', async () => {
        })

        it('TODO: processMessage() should not process a message with an incorrect nonce', async () => {
        })

        it('TODO: processMessage() should not process a message with an incorrect vote weight', async () => {
        })

        it('TODO: processMessage() should not process a message with an incorrect state tree index', async () => {
        })

        it('TODO: processMessage() should not process a message with an incorrect signature', async () => {
        })

        it('TODO: processMessage() should not process a message with an invalid maxVoteOptionIndex', async () => {
        })
    })
})

